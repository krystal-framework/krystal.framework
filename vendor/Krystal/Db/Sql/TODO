Things need to be done
======================

 * More connectors
 * increment\decrements
 * Where IN - support
 * Relations - very common types at least
 * QB for Migrations
 * Improved order by for zeros (which is aware of `if / then`)
 * Raw DB Expressions support